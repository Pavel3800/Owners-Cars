<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="./img/icon-car.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <title><%= title %></title>
</head>
<body>
  
    <%- include('./partials/header'); %>
    <div class="container">
        <h1>Таблица Owners</h1>
          
          <table class="table">
            <thead>
              <tr>
                <th >Id</th>
                <th>Имя</th>
                <th>Фамилия</th>
                <th>Возраст</th>
                <th>Пол</th>
                <th>Дата рождения</th>
              </tr>
            </thead>
            <tbody class="table-group-divider">
              <% for (let i = 0; i < owners.length; i++) { %>
                <tr>
                    <td><%= owners[i].id %></td>
                    <td><%= owners[i].firstName %></td>
                    <td><%= owners[i].lastName %></td>
                    <td><%= owners[i].age %></td>
                    <td><%= owners[i].sex %></td>
                    <td><%= owners[i].birthday.split("-")[2] + "." + owners[i].birthday.split("-")[1] + "." + owners[i].birthday.split("-")[0] %></td>
                </tr> 
            <% } %>
            </tbody>
          </table>
          <h1>Таблица Cars</h1>
          <table class="table">
            <thead>
              <tr>
                <th >Id</th>
                <th>Название машины</th>
                <th>Цвет</th>
                <th>Максимальная скорость</th>
                <th>Цена</th>
              </tr>
            </thead>
            <tbody class="table-group-divider">
              <% for (let i = 0; i < cars.length; i++) { %>
                <tr>
                    <td><%= cars[i].id %></td>
                    <td><%= cars[i].carName %></td>
                    <td><%= cars[i].color %></td>
                    <td><%= cars[i].maxSpeed + ' км/ч' %></td>
                    <td><%= cars[i].price.toLocaleString() + ' ₽' %></td>
                </tr>
            <% } %>
            </tbody>
          </table>
          <h1>Таблица Owners_Cars</h1>
          <table class="table">
            <thead>
              <tr>
                <th>Id_Cars</th>
                <th >Id_Owners</th>
              </tr>
            </thead>
            <tbody class="table-group-divider">
              <% for (let i = 0; i < owners_cars.length; i++) { %>
                <tr>
                    <td><%= owners_cars[i].CarId %></td>
                    <td><%= owners_cars[i].OwnerId %></td>
                </tr>
            <% } %>
            </tbody>
          </table>
    </div>
    
</body>
</html>