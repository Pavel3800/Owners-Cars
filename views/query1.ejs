<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="./img/icon-car.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link rel="stylesheet" href="/home/style.css">
    <title><%= title %></title>
</head>
<body>
   
    <%- include('./partials/header'); %>
    <div class="container">
        <h1>Запрос 1. Список Имен, фамилий, машин и их стоимость</h1>         
          <table class="table">
            <thead>
              <tr>
                <th>ID</th>
                <th>Имя</th>
                <th>Фамилия</th>
                <th>Название машины</th>
                <th>Цена</th>
              </tr>
            </thead>
            <tbody class="table-group-divider">
              <% for (let i = 0; i < querys.length; i++) { %>
                  <tr>
                    <th><%= querys[i].id %></th>
                    <td><%= querys[i].firstName %></td>
                    <td><%= querys[i].lastName %></td>
                    <td>
                      <% for (let j = 0; j < querys[i].Cars.length; j++) { %>
                        
                        <%= querys[i].Cars[j].carName %><br>                                                
                     <% } %>
                    </td>
                    <td>
                      <% for (let j = 0; j < querys[i].Cars.length; j++) { %>
                        
                        <%= querys[i].Cars[j].price.toLocaleString() + ' ₽' %><br>                                                
                     <% } %>
                    </td>
                    
                  </tr>
             <% } %>
            </tbody>
          </table>
            
    </div>
    
    
</body>
</html>